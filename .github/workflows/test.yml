name: Run tests

on:
    push:
        paths:
            - "**.php"
    pull_request:
        paths:
            - "**.php"

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1
            - name: Run tests
              run: |
                  tools/test-github
